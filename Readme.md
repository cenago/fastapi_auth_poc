FastAPI Athentication POC

Installation & git commands:

```commandline
   1 pip install -r requairment.txt
   2 git add .
   3 git status
   4 git commit -m "Added dependency package and auth"
   5 git push --set-upstream origin hello_world

```

```commandline
uvicorn main:app --reload

```